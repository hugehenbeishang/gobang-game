# 五子棋服务器部署指南

## 方案一：Railway（推荐，最简单）

### 第1步：注册Railway账号
1. 访问 https://railway.app
2. 点击「Login」
3. 使用 GitHub 账号登录（没有的话先注册一个）

### 第2步：创建新项目
1. 登录后点击「New Project」
2. 选择「Deploy from GitHub repo」
3. 如果没有代码在GitHub上，选择「Empty Project」

### 第3步：上传代码
如果选择Empty Project：
1. 在Railway项目中点击「New」→「GitHub Repo」
2. 或者直接把 `server` 文件夹拖到Railway

### 第4步：配置环境
Railway会自动检测Node.js项目并部署。

### 第5步：获取域名
1. 在项目设置中找到「Settings」
2. 点击「Networking」→「Generate Domain」
3. 复制生成的域名（类似 `xxx.up.railway.app`）

### 第6步：更新游戏地址
打开 `game.js`，修改第15行：
```javascript
const WS_URL = 'wss://你的域名.up.railway.app';
```
注意：用 `wss://`（不是 `ws://`），因为Railway使用HTTPS。

---

## 方案二：Render（也很简单）

### 第1步：注册Render账号
1. 访问 https://render.com
2. 注册账号（支持GitHub登录）

### 第2步：创建Web Service
1. 点击「New」→「Web Service」
2. 连接GitHub仓库，或选择「Build and deploy from a Git repository」
3. 手动上传代码

### 第3步：配置
- Name: gobang-server
- Runtime: Node
- Build Command: `npm install`
- Start Command: `npm start`

### 第4步：获取域名
Render会自动分配一个域名，格式类似 `xxx.onrender.com`

### 第5步：更新游戏地址
```javascript
const WS_URL = 'wss://你的域名.onrender.com';
```

---

## 方案三：免费云服务器

### 腾讯云 CloudBase
1. 访问 https://cloud.tencent.com/product/tcb
2. 开通云开发（有免费额度）
3. 创建云函数，上传server.js代码
4. 配置WebSocket触发器

### 阿里云函数计算
1. 访问 https://www.aliyun.com/product/fc
2. 创建函数计算服务
3. 上传Node.js代码

---

## 注意事项

1. **协议**：云服务器通常使用HTTPS，所以WebSocket地址要用 `wss://`
2. **端口**：云服务器会自动分配端口，不需要手动指定
3. **免费额度**：Railway每月有5美元免费额度，足够测试使用
4. **持久化**：免费版可能会休眠，第一次访问会慢几秒

---

## 测试部署

部署完成后，可以在浏览器中访问：
```
https://你的域名
```
应该看到 "五子棋WebSocket服务器运行中"

然后在微信开发者工具中更新 `game.js` 的 `WS_URL`，重新编译即可。